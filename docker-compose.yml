version: "3"
services:
  app:
    build: .
    depends_on:
      - postgres
    ports:
      - "3000:3000"
      - "9229:9229"
    env_file: .env.docker
  postgres:
    image: postgres:10.1-alpine
    environment:
      POSTGRES_PASSWORD: postgres